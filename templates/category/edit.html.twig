{% extends 'base.html.twig'%}

{%  block body %}

    <div class="row">
        <div class="col-lg-8 offset-lg-2">
            {# J'affiche mon formulaire #}
            {{ form_start(categoryForm) }}
                    {{ form_label(categoryForm.name, 'Nom de la catégorie') }}
                    {{ form_widget(categoryForm.name) }}

            {{ form_widget(categoryForm) }}

            <button class="btn btn-success my-3" formnovalidate>
                {% if path('category_create') %}
                    Ajouter
                {% elseif path('category_edit') %}
                    Modifier
                {% endif %}
            </button>
            {{ form_end(categoryForm) }}
        </div>
    </div>
{% endblock %}